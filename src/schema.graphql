type Query {
  info: String!
  feed: [Link!]!
  link(id: ID!): Link
}

type Mutation {
  createPost(description: String!, url: String!): Link
  updateLink(linkId: String!, url: String, description: String): Link
  deleteLink(linkId: String!): Link
}

type Link {
  id: ID!
  description: String!
  url: String!
  is_deleted: Boolean!
}